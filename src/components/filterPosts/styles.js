import {makeStyles} from "@material-ui/styles";
const useStyle = makeStyles(theme => ({
   switchFilter: {
      position:"absolute",
      right:210
   },
   formControl:{
      width: "100%!important",

   },
}));

export default useStyle;